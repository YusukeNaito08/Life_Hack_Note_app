    <div class= "col-md-4">
      <div class= "card profile">
        <div class= "card-body">
          <div class= "align-items-center text-center">
            <%= attachment_image_tag @user, :image, :fill, 120, 120, format: 'jpeg', fallback: "no_image.jpg", class:"rounded-circle" %>
            <div class= "mt-3">
              <h4 class="user_name mb-5"><%= @user.name %></h4>
              <% if @user == current_user %>
               <%= link_to  edit_user_path(@user) do %>
               <i class="fas fa-cogs edit"></i>
               <% end %>
              <% end %>
            </div>
          </div>
        </div>
      <div class="card user-card">
        <ul class="list-group list-group-flush mt-5 ">
          <li class="list-group-item d-flex justify-content-between align-items-right">Post <span><%= @user.posts.count %></span></li>
          <li class="list-group-item d-flex justify-content-between align-items-center">Bookmark <%= link_to bookmarks_users_path, class:"bookmark" do %>
            <%= @user.bookmark_posts.count %>
          <% end %></li>
        </ul>
      </div>
      </div>
    </div>
